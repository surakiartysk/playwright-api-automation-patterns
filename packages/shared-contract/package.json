{
  "name": "@gear-rental/shared-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The fictional Gear Rental API: its OpenAPI contract and the in-process mock that implements it. Consumed by every test package so the only variable between them is test-code organization.",
  "exports": {
    ".": "./src/index.ts",
    "./mock": "./src/mock/server.ts"
  },
  "scripts": {
    "start": "tsx src/mock/main.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
